import axios from 'axios';

// Helper function to call the OpenAI API
export const handleFeedback = async (f, currentMessageId) => {
    console.log("handleFeedback-> Feedback received:", f, currentMessageId);    
}